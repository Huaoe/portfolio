# Exported from Render on 2026-03-20T10:33:15Z
# Updated for static export deployment - no server needed
version: "1"
projects:
- name: Porfolio
  environments:
  - name: Production
    services:
    - type: web
      name: portfolio
      runtime: static
      repo: https://github.com/Huaoe/portfolio
      envVars:
      - key: PORT
        sync: false
      buildCommand: yarn --frozen-lockfile install; yarn build
      staticPublishPath: out
      autoDeployTrigger: commit
